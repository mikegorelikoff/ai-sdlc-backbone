"$id": "https://github.com/mikegorelikoff/ai-sdlc-harness/schemas/ai-sdlc-evidence-ledger-v1"
"$schema": ai-sdlc-toon-contract/v1
additionalProperties: false
properties:
  as_of:
    format: date
    type: string
  coverage:
    type: object
  fingerprint:
    pattern: "^[a-f0-9]{64}$"
    type: string
  graph_fingerprint:
    pattern: "^[a-f0-9]{64}$"
    type: string
  records:
    items:
      type: object
    type: array
  schema:
    const: ai-sdlc-evidence-ledger/v1
  source_fingerprint:
    pattern: "^[a-f0-9]{64}$"
    type: string
  stale_paths:
    items:
      type: object
    type: array
required[8]: schema,as_of,graph_fingerprint,source_fingerprint,records,coverage,stale_paths,fingerprint
title: AI SDLC Evidence Ledger
type: object
