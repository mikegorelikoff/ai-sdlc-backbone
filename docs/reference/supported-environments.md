# Supported environments

The public installer requires Node.js 20+ and the packaged install operation
requires Python 3.10+. Supported agent profiles are listed in the
[install guide](../guides/install.md).
